{
	"name": "next-js-13-app-playground-migration-poc",
	"workspaces": [
        "apps/*",
		"packages/*"
	],
	"scripts": {
        "build": "turbo build",
        "pages:build": "turbo pages:build",
        "deploy:worker": "npm --prefix apps/gateway-worker run deploy"
	},
	"devDependencies": {
		"turbo": "^1.10.2"
	}
}